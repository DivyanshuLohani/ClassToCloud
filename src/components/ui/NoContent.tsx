import {View, Text} from 'react-native';
import React from 'react';

const NoContent = () => {
  return (
    <View className="flex items-center justify-center h-full">
      <Text className="text-white">No Content Yet</Text>
    </View>
  );
};

export default NoContent;
